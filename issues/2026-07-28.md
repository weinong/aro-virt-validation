# 2026-07-28 - MSHV guest login failure localizes to CPU feature exposure

## Summary

OpenShift Virtualization compute test `1463` was reproduced in isolation with
the unchanged validation image and diagnostic QEMU `.3` launcher. All three
attempts reached the test's VMI-running condition, then timed out waiting for
an Alpine console login. The first attempt inspected live also reported
`Ready=True`.

Live QMP inspection showed that these VMIs were not booted guests with a broken
console. The only vCPU consumed approximately 65% CPU while its instruction
pointer remained fixed at a SeaBIOS `wrmsr` instruction for
`IA32_FEATURE_CONTROL` (`MSR 0x3a`). The serial log remained empty, the guest
agent stayed disconnected, and QEMU reported zero block reads. The VM had not
reached its boot disk.

A controlled CPU A/B showed that nested-VMX exposure and the tested SGX feature
combination are independent sufficient triggers for the firmware stall in this
configuration. Explicit CPU models without those features reached the Alpine
ISOLINUX bootloader. This provides a concrete explanation for the difference
between the earlier Westmere diagnostic VMI and this `host-model` reproduction.
It does not identify whether MSHV, QEMU, libvirt capability generation, or
another layer is responsible for the non-retiring MSR write.

This is a diagnostic result, not a test workaround or product fix. No validation
test or cluster CPU configuration was changed.

## Environment

```text
Cluster:               weinongw-58dc
OCP:                   4.22.7
CNV CSV:               kubevirt-hyperconverged-operator.4.99.0-2921
KubeVirt:              v1.9.0-beta.0
Hypervisor:            hyperv-direct
Node:                  weinongw-58dc-gj7hc-worker-mshv-centralus1-nmxtk
Node VM size:          Standard_D192ds_v6
Physical CPU:          GenuineIntel, Intel Xeon Platinum 8573C
Launcher QEMU:         qemu-kvm-10.1.0-17.el9_8.3
Launcher digest:       sha256:7c49c99f1506dce2ed93f7018dc69ed118183a116e4f93011f87dbfcb4b1d1c4
Validation image:      sha256:601b6fd9aaab3704757ddb944dca90fddcbaf2135d66e75c435c99ee6a897be4
Alpine disk image:     quay.io/kubevirt/alpine-container-disk-demo:v1.9.0-beta.0
```

## Focused Validation Reproduction

The checkup was generated with:

```text
TEST_SUITES=compute
TEST_FOCUS=test_id:1463
FULL_SUITE=false
```

Run `20260728-155946` selected exactly one spec:

```text
Tests Run:     1
Tests Passed:  0
Tests Failed:  1
Attempts:      3
```

Each attempt failed at the same unmodified test step:

```text
STEP: Waiting for VMI to be running
STEP: Expecting to be able to login
expect: timer expired after 180 seconds
```

The Job completed normally and produced JUnit. This was a test failure rather
than another checkup-pod or node interruption.

## Live Failure State

The first attempt, which was inspected live, used VMI `testvmi-txhzt-...` and
resolved `host-model` to:

```text
-cpu EPYC-Turin,...,vmx=on,...,sgx=on,...,sgx1=on,sgx2=on,...
```

KubeVirt and libvirt reported:

```text
VMI phase:       Running
VMI Ready:       True
libvirt state:   running (booted)
QMP query-status: running=true
serial log:      0 bytes
guest agent:     disconnected
block reads:     0 requests, 0 bytes
```

Three register samples over approximately twelve seconds were identical. `EDX`
was zero, so the complete `EDX:EAX` value is represented by `EAX` below:

```text
EIP=06fee26a
ECX=0000003a
EAX=00060005
```

Disassembly at that address showed:

```text
0x06fee26a: wrmsr
```

The QEMU thread named `CPU 0/MSHV` consumed approximately 68% CPU, while all
three samples remained at the same instruction. This proves that the vCPU did
not retire the `wrmsr` during the observation window. The value `0x60005`
contains the lock bit, VMX-outside-SMX enable, SGX global enable, and SGX Launch
Control enable bits.

The empty console and offline guest agent are therefore downstream symptoms of
an early firmware vCPU stall, not evidence of a console proxy or guest-agent
transport failure.

## CPU Capability Mismatch

The RHCOS host reports an Intel physical CPU:

```text
Vendor ID:       GenuineIntel
Model name:      INTEL(R) XEON(R) PLATINUM 8573C
```

The same launcher's libvirt `hyperv` domain capabilities report:

```xml
<mode name='host-model' supported='yes'>
  <model fallback='forbid'>EPYC-Turin</model>
  <vendor>AMD</vendor>
  ...
</mode>
```

That host model includes a very broad mixed-vendor feature list, including
VMX, SGX, Intel PT, and AMD SVM-related features. KubeVirt publishes the same
physical-host/domain-capability inconsistency on the node:

```text
cpu-vendor.node.kubevirt.io/AMD=true
host-model-cpu.node.kubevirt.io/EPYC-Turin=true
host-model-required-features.node.kubevirt.io/vmx=true
host-model-required-features.node.kubevirt.io/sgx=true
```

The meaning and source of this host-model result remain open. The evidence
shows that the validation test consumes the QEMU/libvirt capabilities through
KubeVirt node labels; it does not establish which layer generated an incorrect
value or whether every advertised feature is invalid.

## Diagnostic A/B Matrix

All controls used the same node, MSHV accelerator, QEMU `.3` launcher, q35
machine type, 128 MiB memory, virtio disk, and Alpine image.

| CPU definition | Result |
|---|---|
| `Westmere` | Reached ISOLINUX |
| `EPYC-Turin` | Reached ISOLINUX |
| `EPYC-Turin,vmx=on` | Stuck at `wrmsr MSR 0x3a`, value `0x5`; zero disk reads |
| `Westmere,vmx=on` | Stuck at `wrmsr MSR 0x3a`, value `0x5`; zero disk reads |
| `EPYC-Turin,sgx=on,sgx1=on,sgx2=on` | Stuck at `wrmsr MSR 0x3a`, value `0x40001`; zero disk reads |
| `host-passthrough` | Stuck at `wrmsr MSR 0x3a`, value `0x60005`; zero disk reads |
| `host-model,vmx=off` | Still stuck; SGX controls produced value `0x60001` |
| `host-model,vmx=off,sgx*=off` | Reached ISOLINUX |

The matrix demonstrates:

1. The base `EPYC-Turin` model is not sufficient to cause the stall.
2. Exposing `vmx` is sufficient, independent of whether the base model is Intel
   Westmere or AMD EPYC-Turin.
3. Exposing the tested `sgx+sgx1+sgx2` combination is independently sufficient.
4. Removing both top-level VMX and SGX exposure lets the otherwise expanded
   host model reach the bootloader.

The remaining nested-VMX subfeatures were still present in the positive
`host-model,vmx=off,sgx*=off` command. SeaBIOS did not enter the failing
`IA32_FEATURE_CONTROL` path because the top-level capabilities were disabled.

## Relationship to Earlier Failures

This finding provides a tested mechanism consistent with the repeated pattern
from the July 25 run:

- VMI `Running` and often `Ready=True`.
- QEMU process remains alive.
- Empty guest console.
- Guest agent never connects.
- Guest-dependent tests time out before their named assertion.

The earlier VMIs were not inspected at the register and block-I/O level, so the
same mechanism is not proven for every prior failure. Migration destination
constraints and the separate host TX-queue warnings remain independent open
findings. This result also does not prove the cause of the two earlier node
restarts.

## Full Compute Test Matrix

The matrix below accounts for all 61 compute specs selected in run
`20260725-032300`. The raw log contains 53 terminal Ginkgo markers: 34 passed
and 19 failed. One additional spec was in a retry when the MSHV node restarted,
and the final seven specs never started.

The complete selection and order were reconstructed with a read-only Ginkgo
dry run using the same validation binary, random seed `1784949802`, quarantine
list, downstream label exclusions, `sig-compute&&conformance`, and
`!RequiresRWXBlock` filter. The reconstructed selection reported the same
`61 of 1401 specs` as the interrupted run.

Issue mapping keys:

| Key | Meaning |
|---|---|
| `CPU-MSR confirmed` | Focused rerun directly observed the non-retiring `IA32_FEATURE_CONTROL` write, empty serial log, and zero disk reads. |
| `CPU-MSR consistent` | Failed at console, login, or guest-agent prerequisites with the same downstream pattern. The MSR state was not directly inspected for that July 25 VMI. |
| `MIG-PLACEMENT candidate` | Reached migration placement or destination behavior. The cluster had one MSHV-role node, but no scheduler rejection proved that topology caused the failure. |
| `NEW/UNRESOLVED` | Evidence does not map safely to a documented mechanism and needs a focused investigation. |
| `None` | The spec passed, or did not produce a result to classify. |

| # | Test ID(s) | Test | Status | Issue mapping |
|---:|---|---|---|---|
| 1 | `8607` | vTPM persists across migrations | **FAIL** | `CPU-MSR consistent`: Fedora login failed before TPM or migration assertions. |
| 2 | `1611` | Access VNC device multiple times | **PASS** | None |
| 3 | `1463` | Start and stop a ContainerDisk VM repeatedly | **FAIL** | `CPU-MSR confirmed`: focused rerun reproduced the firmware MSR stall. |
| 4 | `3242` | Eviction API blocks and migrates VMI | **FAIL** | `MIG-PLACEMENT candidate`: VMI did not move from its source node; exact cause unresolved. |
| 5 | `4300` | Validate documented virt-launcher capabilities | **FAIL** | `CPU-MSR consistent`: Alpine login failed before capability assertion. |
| 6 | `4598`, `3060` | Signal paused and unpaused VM state | **PASS** | None |
| 7 | `3085` | Stop paused VM successfully | **PASS** | None |
| 8 | `6220` | Add SSH key through QEMU guest agent | **FAIL** | `CPU-MSR consistent`: guest agent offline; named assertion not reached. |
| 9 | `6224` | Add SSH key through config drive | **FAIL** | `CPU-MSR consistent`: `AgentConnected` never appeared. |
| 10 | - | Add SSH key through NoCloud | **FAIL** | `CPU-MSR consistent`: `AgentConnected` never appeared. |
| 11 | `3161` | Propagate template annotations to VMI | **PASS** | None |
| 12 | - | Synchronize VMI generation annotation on restart | **PASS** | None |
| 13 | - | Do not update VMI generation when template changes | **PASS** | None |
| 14 | `1521` | Remove ContainerDisk VMI when VM is deleted | **PASS** | None |
| 15 | `1521` | Remove filesystem-disk VMI when VM is deleted | **PASS** | None |
| 16 | `1522` | Remove VMI owner reference after orphan deletion | **PASS** | None |
| 17 | `1523` | Recreate VMI after VMI deletion | **PASS** | None |
| 18 | `1524` | Recreate VMI after launcher pod deletion | **PASS** | None |
| 19 | `1526` | Start and stop VMI multiple times | **PASS** | None |
| 20 | `1527` | Do not update running VMI specification | **PASS** | None |
| 21 | `1528` | Survive repeated guest shutdown | **FAIL** | `CPU-MSR consistent`: initial Alpine login failed before shutdown behavior. |
| 22 | - | Keep VM revision updated on start | **PASS** | None |
| 23 | `4645` | Set VM Ready condition | **PASS** | None |
| 24 | `6867` | Report unsatisfiable resource scheduling error | **PASS** | None |
| 25 | `6868` | Report unsatisfiable scheduling constraint | **PASS** | None |
| 26 | `7596` | Report missing PVC error | **PASS** | None |
| 27 | `7597` | Report missing DataVolume error | **PASS** | None |
| 28 | `6869` | Report image pull error | **PASS** | None |
| 29 | `299`, `264` | Create and delete VM through `v1` API | **PASS** | None |
| 30 | `299`, `264` | Create and delete VM through `v1alpha3` API | **PASS** | None |
| 31 | - | Back off VM creation during crash loop | **PASS** | None |
| 32 | - | Add and remove VM controller finalizer | **PASS** | None |
| 33 | - | Remove finalizer after child revision deletion | **PASS** | None |
| 34 | `3110` | Assign Burstable QoS when requests and limits differ | **PASS** | None |
| 35 | - | Port-forward to guest SSH | **FAIL** | `NEW/UNRESOLVED`: `no route to host`; guest boot, guest networking, or OVN remain possible. |
| 36 | - | Resolve VM IP through headless service during migration | **FAIL** | `CPU-MSR consistent`: guest login failed before server or migration setup. |
| 37 | `6969` | Migrate VMI with tablet device | **FAIL** | `CPU-MSR consistent`: initial Alpine login failed before migration. |
| 38 | `6970` | Migrate VMI with CD-ROM bus variants | **FAIL** | `CPU-MSR consistent`: initial Alpine login failed before migration. |
| 39 | `1783` | Repeated migration with cloud-init disk | **FAIL** | `CPU-MSR consistent`: initial Alpine login failed before migration. |
| 40 | - | Cancel unschedulable migration target after timeout | **PASS** | None |
| 41 | `1853` | Migrate SATA and mixed-disk VMI | **FAIL** | `MIG-PLACEMENT candidate`: migration remained in `Scheduling` for 240 seconds; exact cause unresolved. |
| 42 | - | Allow only one active migration per VMI | **PASS** | None |
| 43 | - | Report migration ResourceQuota rejection | **PASS** | None |
| 44 | `6095` | Start VMI paused and unpause it | **PASS** | None |
| 45 | `3196` | Propagate Kubernetes and KubeVirt annotations to pod | **PASS** | None |
| 46 | - | Prevent eviction with external eviction strategy | **PASS** | None |
| 47 | `7480` | Freeze and unfreeze VMI repeatedly | **FAIL** | `CPU-MSR consistent`: `AgentConnected` never appeared; freeze was not reached. |
| 48 | - | Automatically unfreeze after timeout | **FAIL** | `CPU-MSR consistent`: `AgentConnected` never appeared. |
| 49 | - | Soft reboot with connected guest agent | **FAIL** | `CPU-MSR consistent`: `AgentConnected` never appeared; reboot was not reached. |
| 50 | - | Soft reboot with ACPI enabled | **FAIL** | `CPU-MSR consistent`: Fedora login failed before reboot. |
| 51 | - | Reject soft reboot without agent or ACPI | **FAIL** | `CPU-MSR consistent`: Alpine login failed before rejection assertion. |
| 52 | `4597` | Signal VMI paused-state condition | **PASS** | None |
| 53 | `1650` | Finalize VMI after launcher pod deletion | **PASS** | None |
| 54 | `1653` | Graceful deletion with explicit ACPI grace period | **INTERRUPTED** | `CPU-MSR consistent` symptom on attempt 1; attempt 2 was active when the node restarted, so there is no terminal result. |
| 55 | `1654` | Graceful deletion with default ACPI grace period | **NOT RUN** | None; selected but never started. |
| 56 | `1655` | Run graceful shutdown with nonzero grace period | **NOT RUN** | None; selected but never started. |
| 57 | `1591` | Replace an existing serial-console connection | **NOT RUN** | None; selected but never started. |
| 58 | `4118` | Reject console connection when no serial console exists | **NOT RUN** | None; selected but never started. |
| 59 | `1618` | Consume NoCloud user data from Secret | **NOT RUN** | None; selected but never started. |
| 60 | `3178` | Consume ConfigDrive base64 user data | **NOT RUN** | None; selected but never started. |
| 61 | `4641` | Shut down VMI when watchdog expires | **NOT RUN** | None; selected but never started. |

Matrix totals:

```text
PASS:                 34
FAIL:                 19
  CPU-MSR confirmed:   1
  CPU-MSR consistent: 15
  MIG-PLACEMENT candidates: 2
  NEW/UNRESOLVED:      1
INTERRUPTED:            1
NOT RUN:                7
SELECTED:              61
```

The pass results are also useful boundaries. API lifecycle, metadata,
finalizer, error-reporting, paused-state, quota-rejection, and several
migration-controller behaviors worked without requiring a responsive guest.
Most failures depended on firmware reaching the guest OS. The two migration
failures reached placement or destination behavior but remain open candidates,
not confirmed topology issues. The port-forward result is the only other
completed failure that does not map safely to the CPU mechanism.

## Manual Diagnostic Interventions

- Ran the unmodified validation test with a focus expression selecting only
  test `1463`. This was a diagnostic reproduction and not a replacement
  validation result.
- Used read-only `virsh` and QMP queries inside the failing launcher pod.
- Created temporary A/B VMIs with explicit CPU definitions.
- The CPU-feature-disabled VMIs were diagnostic controls only. No validation
  test was modified or rerun with those settings, and they are not described as
  fixes.
- Ran the validation binary locally in Ginkgo dry-run mode with the original
  seed and filters to reconstruct the identities and order of all 61 selected
  specs. The dry run did not connect to the cluster or create workloads.
- Deleted the temporary A/B namespace and reader pod after preserving evidence.
- The persistent unsupported launcher override, previously applied through the
  CNV CSV/virt-operator and HCO pull-secret path because the shipped QEMU
  immediately aborts on MSHV, remained active and unchanged.

## Conclusion

For focused test `1463`, the guest-console timeout occurs because firmware does
not progress past an `IA32_FEATURE_CONTROL` write, not because the console
channel fails. In this configuration, VMX exposure or the tested SGX feature
combination is sufficient to reproduce the non-retiring write before any
boot-disk I/O. The normal `host-model` and `host-passthrough` paths expose those
features and reproduce the stall.

The responsible implementation layer remains open. A product resolution must
make normal CPU selection boot reliably under MSHV without per-VMI diagnostic
feature overrides.

## Artifacts

The focused Job and PVC are retained in namespace `ocp-virt-validation` as:

```text
ocp-virt-validation-job-20260728-155946
ocp-virt-validation-pvc-20260728-155946
```
