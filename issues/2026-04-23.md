# HCO patch: hyperv-direct successfully applied

**Date:** 2026-04-23
**CNV version:** 4.99.0-2759 (nightly, auto-upgraded from 4.99.0-2739)
**OCP version:** 4.22.0-rc.0
**Cluster:** aro-virt-test

## Summary

The HCO jsonpatch for `hyperv-direct` was successfully applied after fixing the field name from `hypervisorConfiguration` (object) to `hypervisors` (array). The correct patch entry is:

```json
{
  "op": "add",
  "path": "/spec/configuration/hypervisors",
  "value": [ { "name": "hyperv-direct" } ]
}
```

All fields are now applied:

| Field | Value | Status |
|-------|-------|--------|
| `developerConfiguration.featureGates` | includes `ConfigurableHypervisor` | Applied |
| `cpuModel` | `qemu64-v1` | Applied |
| `evictionStrategy` | `None` | Applied |
| `obsoleteCPUModels.qemu64` | `false` | Applied |
| `obsoleteCPUModels.qemu64-v1` | `false` | Applied |
| `hypervisors[0].name` | `hyperv-direct` | Applied |

## Node state

The MSHV node is correctly configured:

```
Node:    aro-virt-test-m4ftb-worker-mshv-centralus1-px6tl
OS:      RHCOS 10.2.20260408-0
Kernel:  6.12.0-211.7.1.el10_2.x86_64
L1VH:    Yes (dmesg: "Hyper-V: running as L1VH partition")
VM SKU:  Standard_D192ds_v6
```

## D8s_v5 machinesets deleted

All three D8s_v5 worker machinesets (`centralus1`, `centralus2`, `centralus3`) were deleted entirely (not scaled to 0) since ARO continuously scales them back up.

## Next steps

- Run `scripts/05-cnv-validation-checkup.sh` to validate hyperv-direct on the L1VH node
