# HCO patch and validation: hyperv-direct on MSHV/L1VH

**Date:** 2026-04-23
**CNV version:** 4.99.0-2759 (nightly, auto-upgraded from 4.99.0-2739)
**OCP version:** 4.22.0-rc.0
**Cluster:** aro-virt-test

## Summary

VMs created under the `hyperv-direct` hypervisor crash immediately at boot.
The root cause is that the RHCOS 10.2 kernel's `mshv_root` module rejects
`MSHV_CREATE_PARTITION` ioctls as "unsupported", despite the node running in
L1VH partition mode. Additionally, `Nested features: 0x0` indicates that the
Azure host is not exposing nested virtualization features, even though L1VH
mode is active.

## Blocking issue: MSHV_CREATE_PARTITION ioctl unsupported

### Symptoms

Every VMI created with `hyperv-direct` follows this lifecycle:

```
Scheduled → Paused (StartingUp) → Running → Shutoff (Crashed) → Failed
```

The VM starts momentarily then crashes within ~1 second.

### dmesg evidence

```
[  527.932241] using unsupported MSHV_CREATE_PARTITION ioctl
[  529.091679] using unsupported MSHV_CREATE_PARTITION ioctl
...
(repeated for every VM creation attempt)
```

### L1VH status

```
Hyper-V: running as L1VH partition
Hyper-V: Nested features: 0x0         <-- no nested virt features exposed
```

Despite running as L1VH, `Nested features: 0x0` means the Azure host is not
enabling the nested virtualization features that `mshv_root` needs to create
child partitions. The `MSHV_CREATE_PARTITION` ioctl is present in the kernel
module but gated — it logs "unsupported" and the partition creation fails.

### Module info

```
filename:       /lib/modules/6.12.0-211.7.1.el10_2.x86_64/kernel/drivers/hv/mshv_root.ko.xz
description:    Microsoft Hyper-V root partition VMM interface /dev/mshv
intree:         Y
vermagic:       6.12.0-211.7.1.el10_2.x86_64 SMP preempt mod_unload modversions
```

### Device plugin status

Both `/dev/mshv` and `/dev/vhost-net` are present and advertised by virt-handler:

```
devices.kubevirt.io/mshv:      1k
devices.kubevirt.io/vhost-net:  1k
devices.kubevirt.io/kvm:        0
```

### Node labels

```
Node:    aro-virt-test-m4ftb-worker-mshv-centralus1-px6tl
         aro-virt-test-m4ftb-worker-mshv-centralus1-mmnsx
OS:      RHCOS 10.2.20260408-0 (Coughlan)
Kernel:  6.12.0-211.7.1.el10_2.x86_64
L1VH:    Yes
VM SKU:  Standard_D192ds_v6
```

## HCO patch

The HCO jsonpatch for `hyperv-direct` was successfully applied after fixing
the field name from `hypervisorConfiguration` (object) to `hypervisors` (array).

### cpuModel issue (fixed)

Setting `cpuModel: qemu64-v1` in the HCO config causes KubeVirt to add
`cpu-model.node.kubevirt.io/qemu64-v1=true` as a nodeSelector on every VMI
launcher pod. Since `qemu64-v1` is a virtual QEMU CPU model (not a real CPU
model that virt-handler advertises on nodes), VMIs could never schedule.

**Fix:** Removed `cpuModel` and `obsoleteCPUModels` from the HCO jsonpatch.

### Current HCO jsonpatch

```json
[
  {
    "op": "add",
    "path": "/spec/configuration/developerConfiguration/featureGates",
    "value": ["ConfigurableHypervisor", "CPUManager", "Snapshot", "HotplugVolumes",
              "ExpandDisks", "HostDevices", "VMExport", "KubevirtSeccompProfile",
              "VMPersistentState", "InstancetypeReferencePolicy", "WithHostModelCPU",
              "HypervStrictCheck"]
  },
  {
    "op": "add",
    "path": "/spec/configuration/hypervisors",
    "value": [ { "name": "hyperv-direct" } ]
  },
  {
    "op": "add",
    "path": "/spec/configuration/evictionStrategy",
    "value": "None"
  }
]
```

## mshv_root module loading (fixed)

The `mshv_root` kernel module is not auto-loaded at boot. A MachineConfig
(`99-mshv-root-module`) was created to load it via `/etc/modules-load.d/mshv-root.conf`.

The MCO drain was stuck because this is a single-worker-node cluster — PDBs on
single-replica deployments (image-registry, router, prometheus, alertmanager, etc.)
prevent eviction when there's no other node to reschedule to. Fixed by force-deleting
the blocking pods to allow the drain and reboot to proceed.

## Previous validation results are BOGUS

The validation checkup tool (ocp-virt-validation-checkup) reports incorrect results.
When the Ginkgo `SynchronizedBeforeSuite` fails, the tool still reports the
_intended_ test count as "passed":

| What the tool reports | What actually happened |
|----------------------|----------------------|
| 1019 passed, 0 failed | `Ran 0 of 1380 Specs` — zero tests ran |
| 152 passed, 0 failed | `Ran 0 of 1380 Specs` — zero tests ran |

The `SynchronizedBeforeSuite` failure was: "Both mshv and vhost-net devices are
required for testing, but are not present on cluster nodes" — because `/dev/mshv`
did not exist until `mshv_root` was loaded via MachineConfig.

After fixing the module loading, the BeforeSuite passes but VMs crash (see above).

## Next steps

1. Investigate why `Nested features: 0x0` despite L1VH mode — is the Azure VM tag
   `platformsettings.host_environment.nodefeatures.hierarchicalvirtualizationv1=True`
   sufficient, or is an additional host-level enablement needed?
2. Engage Red Hat / Azure host team on the `MSHV_CREATE_PARTITION` ioctl being
   flagged as "unsupported" in the RHCOS 10.2 kernel
3. Check if a newer kernel or kernel module version resolves this
